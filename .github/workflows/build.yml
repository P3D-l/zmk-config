name: "Build"

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/build.yml"
      - "config/**"

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v1.0
